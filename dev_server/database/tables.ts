import {Connection} from './connection'
import Sequelize, {DataTypes} from 'sequelize'
export const TableName = Connection.define('tableName', {
    //here you will but the confgratin of the table
})
